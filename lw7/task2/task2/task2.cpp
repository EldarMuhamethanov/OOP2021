#include <iostream>
#include "CMyList.h"

using namespace std;

int main()
{
    CMyList<char> list;
    /*list.Append(1);
    list.Append(2);
    list.Append(3);
    list.Append(4);

    auto it = list.begin();
    while (it != list.end())
    {
        cout << *it << endl;
        ++it;
    }
    cout << endl;

    auto it1 = list.rbegin();
    while (it1 != list.rend())
    {
        cout << *it << endl;
        ++it;
    }*/
}
